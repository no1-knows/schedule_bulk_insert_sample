<%= form_with(model: schedule_user) do |form| %>
  <% if schedule_user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(schedule_user.errors.count, "error") %> prohibited this schedule_user from being saved:</h2>

      <ul>
        <% schedule_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :schedule_id, style: "display: block" %>
    <%= form.text_field :schedule_id %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
